# Copy to .env and fill in real values. Do not commit .env.
# ARROW_API_URL is optional; default is http://localhost:8000/api/predict
ARROW_API_URL=http://localhost:8000/api/predict
# Use a real secret in .env; this is a placeholder for .env.example only
PROXY_SECRET=REPLACE_ME
